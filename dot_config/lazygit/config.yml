git:
  pagers:
    - pager: delta --dark --paging=never --line-numbers --hyperlinks --hyperlinks-file-link-format="lazygit-edit://{path}:{line}" --side-by-side --minus-style="bold red"  --keep-plus-minus-markers
promptToReturnFromSubprocess: false

